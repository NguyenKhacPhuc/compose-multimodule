package com.example.ui_common
/**
 * This class is used to specify the appropriate ui action for each screen.
 *
 */
enum class UiAction {
    OK,
    BACK,
    EMPTY,
    CLEAR,
    CANCEL,
    TIMEOUT,
    UI_CREATED,
    MANUAL_ENTRY,
    CLICK_VI_EYE_ICON,
    SELECT_OPTION,
    SELECT_ITEM,
    SELECT_ALL,
    SKIP,
    RETRY,
    REPRINT,
    VOID,
    CONFIRM,
    PIN_PAD_CREATED,
    OTHER
}